import StyledHome from './StyledHome';
import { auth } from '../../firebase';

const Home = () => {
  return (
    <StyledHome>
      <p>@@@@@@@@@@@@@@@@@@@ page</p>
      <p>alona laskart</p>
    </StyledHome>
  );
};

export default Home;
