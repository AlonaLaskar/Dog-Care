import StyledProfile from './StyledProfile';
import { IonContent } from '@ionic/react';

const Profile = () => {
  return (
    <StyledProfile>
      <h1>Profie</h1>
    </StyledProfile>
  );
};

export default Profile;
